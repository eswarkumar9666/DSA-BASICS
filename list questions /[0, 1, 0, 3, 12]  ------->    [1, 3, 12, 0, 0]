q)Move all zeros to the end without changing the order of non-zero elements.

Input:

[0, 1, 0, 3, 12]


Output:

[1, 3, 12, 0, 0]


sol) li = [0, 1, 0, 3, 12]
zeros = 0
for i in li:
    if i == 0:
        li.remove(i)
        zeros += 1
li.extend([0] * zeros)
print(li)
